document.addEventListener('DOMContentLoaded', () => {
    const contactButton = document.querySelector('.hero-text button');
    contactButton.addEventListener('click', () => {
        alert('Contact us button clicked!');
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const contactButton = document.querySelector('.hero-text button');
    contactButton.addEventListener('click', () => {
        alert('Contact us button clicked!');
    });
});

